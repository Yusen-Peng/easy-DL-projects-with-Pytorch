version: 2

build:
  os: ubuntu-20.04  # Change to windows-2019 if needed
  tools:
    python: "3.8"  # Specify the Python version

sphinx:
  configuration: docs/conf.py

python:
  install:
    - requirements: docs/requirements.txt